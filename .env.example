# Copy to .env before `docker compose up` (compose reads .env automatically).
# These are local dev defaults — use real secrets in any deployed environment.
POSTGRES_USER=flowgate
POSTGRES_PASSWORD=flowgate_dev
POSTGRES_DB=flowgate
DATABASE_URL=postgres://flowgate:flowgate_dev@localhost:5432/flowgate
